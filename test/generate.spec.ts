describe('stub', () => {
  // Read more about fake timers:
  // http://facebook.github.io/jest/docs/en/timer-mocks.html#content
  jest.useFakeTimers();

  beforeAll(async () => {});

  it('', () => {
    expect(1).toBe(1);
  });
});
